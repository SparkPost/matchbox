import UpdatesIndex from '../components/UpdatesIndex';
import Layout from '../components/Layout/Layout';
export default (props) => <Layout maxWidth="1200" {...props} />;

### What's new

The latest news and updates to the Matchbox design system.

Looking for release notes? We've moved them to our <ExternalLink to="https://github.com/SparkPost/matchbox/releases">Github repository</ExternalLink>.

---

<UpdatesIndex />
