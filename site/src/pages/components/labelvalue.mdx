---
title: LabelValue
category: Component
---

import { LiveCode } from '../../components/LiveCode';

# Page

---

### Usage

<LiveCode>
  <LiveCode.Content
    title="Vertical Orientation"
    code={`
      <LabelValue>
        <LabelValue.Label>Label</LabelValue.Label>
        <LabelValue.Value>Just a LabelValue</LabelValue.Value>
      </LabelValue>
    `}
  />
  <LiveCode.Content
    title="Horizontal Orientation"
    code={`
      <LabelValue orientation="horizontal">
        <LabelValue.Label>Label</LabelValue.Label>
        <LabelValue.Value>Just a LabelValue</LabelValue.Value>
      </LabelValue>
    `}
  />
</LiveCode>

---

### System Props

<SystemPropsTags propsList={['margin']} />

---

### LabelValue API

<Prop name="children" type="React node">
  Contents of the component
</Prop>
<Prop name="className" type="string">
  CSS class name on the wrapping DOM element
</Prop>
<Prop
  name="orientation"
  type="string"
  values={['"vertical"', '"horizontal"']}
  defaultValue='"vertical"'
>
  Orientation of the component
</Prop>
<Prop
  name="appearance"
  type="string"
  values={['"inverted"']}
  defaultValue='"default"'
>
  The appearance of the Label and Value. `inverted` = light text on dark background.
</Prop>

### LabelValue.Label API

<Prop name="children" type="React node">
  Label content
</Prop>

### LabelValue.Value API

<Prop name="children" type="React node">
  Value content
</Prop>
