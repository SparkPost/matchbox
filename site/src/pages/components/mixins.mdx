---
title: Mixins
category: Resource
---

# Mixins

---

### buttonReset

Used to reset button styles to a baseline.

```jsx
import { styles } from '@sparkpost/matchbox';

const CustomButtom = styled.button`
  ${styles.buttonReset}
`;
```

---

### visuallyHidden

Used to hide an element visually, while still available to screen readers or assistive technology.

```jsx
import { styles } from '@sparkpost/matchbox';

const Hide = styled.div`
  ${styles.visuallyHidden}
`;
```

---

### focusOutline

```jsx
import { styles } from '@sparkpost/matchbox';

const Button = styled.button`
  ${styles.focusOutline(options)}
`;
```

<Box mb="800" />

##### focusOutline Options Object

<Prop name="color" type="string" defaultValue="tokens.color_blue_700">
  Color of the focus ring
</Prop>
<Prop name="modifier" type="string" defaultValue="'&:focus'">
  Selector to target focus
</Prop>
<Prop name="offest" type="string" defaultValue="'3px'">
  Distance of the focus ring to the element
</Prop>
