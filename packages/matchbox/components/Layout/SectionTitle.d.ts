export default SectionTitle;
declare function SectionTitle(props: any): any;
declare namespace SectionTitle {
    const propTypes: {
        as: any;
        'data-id': any;
    };
    namespace defaultProps {
        const as: string;
    }
    const displayName: string;
}
