import tokens from '@sparkpost/design-tokens/dist/index.meta.json';
import SpacingPreview from '../../components/SpacingPreview/SpacingPreview';

# Space

Spacing tokens make it easy to configure a component's padding, margin, or position. Consistent spacing promotes a visual balance that helps make UIs more digestible and scannable.

Spacing variants use the same naming convention as font weight, like [typography tokens](design/typography) – three digit names, with <Token name='spacing-400' /> acting as the default.

---

<SpacingPreview />

<small>This scale operates on a 4px baseline, with each variant being a multiple of that baseline.</small>

---

<TokenTable
  columns={['friendly', 'value', 'pixel_value', 'usage']}
  tokens={tokens.filter(({ category }) => category === 'spacing' )}
/>

---
