import UpdatesIndex from '../components/UpdatesIndex';
import Layout from '../components/Layout/Layout';
export default props => <Layout maxWidth="1300" {...props} />;

### What's new

The latest news, updates, and changes to the Matchbox design system.

---

<UpdatesIndex />
